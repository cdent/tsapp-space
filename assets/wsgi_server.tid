type: text/x-markdown

The name of a python module containing a `start_server` method which, when passed `config`, will start a web server hosting the proxy application. By default this is a built in `wsgiref` sever but it can be any `WSGI` server. See [a simple cherrypy example](https://gist.github.com/cdent/5564147).
